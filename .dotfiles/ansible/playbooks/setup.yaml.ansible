---
- name: Setup dev environment
  hosts: localhost

  tasks:
    - name: Install dependencies and nvim for Ubuntu
      block:
      - include: apt.yaml.ansible
      - include: nvim.yaml.ansible
      when: ansible_distribution == "Ubuntu"

